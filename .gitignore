# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local env — `.env` IS tracked because it only contains Supabase publishable
# (anon) keys, which are designed to be public in the browser bundle.
# Use `.env.local` for any actual secrets you don't want committed.
.env.local
.env.*.local
.env.production
.env.production.local

# Lock files (keep one — bun.lock)
# package-lock.json is intentionally kept tracked for npm users
